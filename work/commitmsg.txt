blend: save the .blend on a neutral action pairing

The .blend shipped saved with the weapon parked on A_W_Tac_Reload (arms on
A_FP_Tac_Reload), which is what an export that skips work/export_clip.py bakes
against -- the root cause of the broken 4:00:42 batch in UPDATE 6. User
approved this specific change to animation data, per the standing rule of
asking first.

Before:  Armature -> A_FP_Tac_Reload   AK -> A_W_Tac_Reload
After:   Armature -> A_FP_Idle         AK -> A_W_ADS

Those two are the fallback pairing FPS_ANIMATION_SYSTEM.md already documents:
A_W_ADS is the weapon's bind pose, which is what the engine renders alongside
Idle/Walk/Aim/Draw/Regrip. A naive export now bakes something sane instead of
TacReload's magazine swap.

Verification (reopened in a fresh Blender process after the save):
- pose_position=POSE on both armatures; all 35 NLA Action Stash tracks still
  muted (25 arms + 10 weapon), so only animation_data.action matters
- fingerprint unchanged: objects=130 meshes=122 armatures=2 actions=26
  materials=10, frames 0..275 @ 60 -- 26 actions, so nothing created, dropped
  or re-keyed
- action_slot bound and evaluating: hand_l lands 76.8 rig-units (~0.77 m) from
  rest under A_FP_Idle, where an unbound action reads exactly 0 (and this rig's
  bind pose is the Manny T-pose)
- file 305,729,203 -> 305,729,971 bytes (+768), still zstd

Rollback: AKS-74U 60fps (Revised).blend.pre-neutral.bak, a size-verified copy
taken before the write, next to the original on the Desktop.

Caveat recorded in the docs: the file is zstd-compressed so the saved-with
version header isn't readable from stdlib; if the original came from an older
Blender major, this save upgraded the format in place -- which is what the
backup is for.

New scripts (committed since the docs reference them):
- work/inspect_blend.py      read-only state dump
- work/preflight_neutral.py  compression/version, pose_position, fingerprint
- work/set_neutral_action.py the only script that saves the .blend

Docs: FPS_ANIMATION_INVESTIGATION.md UPDATE 6 gains "Closing the door" and no
longer claims the file is still saved on A_W_Tac_Reload; FPS_ANIMATION_SYSTEM.md
§2 records the neutral saved state and §7 lists the three scripts.
